## 1. 窄屏媒体查询

- [x] 1.1 在 CSS 末尾新增 `@media (max-width: 600px)` 断点块
- [x] 1.2 断点内覆盖 `body` padding：水平收窄为 `12px`，顶部减小为 `24px`
- [x] 1.3 断点内覆盖 `#search-form`：`grid-template-columns: 1fr`，引擎按钮组自然堆叠到搜索框下方
- [x] 1.4 断点内覆盖 `#main-grid`：`grid-template-columns: 1fr`，待办卡片堆叠到书签卡片下方
