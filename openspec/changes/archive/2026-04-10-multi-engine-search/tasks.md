## 1. HTML 结构改造

- [x] 1.1 移除搜索区域原有的单"搜索"按钮
- [x] 1.2 添加三个引擎按钮（百度、Bing、Google），紧排在搜索输入框右侧

## 2. CSS 样式

- [x] 2.1 为默认引擎按钮添加高亮样式（accent 色），非默认为普通样式
- [x] 2.2 确保三按钮在正常宽度下单行显示，间距合理

## 3. JS 逻辑改造

- [x] 3.1 更新默认配置中的引擎列表，固定百度（`bd`）、Bing（`b`）、Google（`g`）三个引擎
- [x] 3.2 移除快捷词解析逻辑
- [x] 3.3 为三个按钮各绑定点击事件：使用对应引擎搜索 + 更新 defaultEngine + 持久化 + 刷新高亮
- [x] 3.4 Enter 键搜索改为读取当前 defaultEngine 执行搜索（移除快捷词分支）
- [x] 3.5 页面初始化时根据 `defaultEngine` 设置对应按钮的高亮样式
